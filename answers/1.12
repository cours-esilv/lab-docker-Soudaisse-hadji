docker run -d --name back-container my-back-image
BACK_IP=$(docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' back-container)
docker run -d --name front-container -e WS_BACK_URL="http://$BACK_IP:5000" frontend-image